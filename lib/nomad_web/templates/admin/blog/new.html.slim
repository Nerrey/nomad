.page-header
  h1.clearfix
    .pull-left= gettext("Новый пост")
    .pull-right
      = link to: admin_blog_path(@conn, :index), class: "btn btn-default" do
        span.glyphicon.glyphicon-list.right_10 aria-hidden="true"
        = gettext("Все посты")

= render "form.html", changeset: @changeset, conn: @conn, action: admin_blog_path(@conn, :create)
